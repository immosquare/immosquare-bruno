meta {
  name: List Property Animals
  type: http
  seq: 25
}

get {
  url: {{baseUrl}}/v1/odata/PropertyAnimal?$top=20&$count=true
  body: none
  auth: bearer
}

params:query {
  $top: 20
  $count: true
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Liste les animaux associés aux propriétés (fermes).
}
