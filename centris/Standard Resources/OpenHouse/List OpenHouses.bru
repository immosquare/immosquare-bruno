meta {
  name: List OpenHouses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/odata/OpenHouse?$top=10&$count=true
  body: none
  auth: bearer
}

params:query {
  $top: 10
  $count: true
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  Liste les visites libres (Open Houses).
}
