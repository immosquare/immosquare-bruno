meta {
  name: agency check property
  type: http
  seq: 8
}

post {
  url: https://{{baseurl}}/v1/logiciel/agency/properties/check?agency_login={{agency_login}}&agency_pass={{agency_pass}}
  body: none
  auth: none
}

query {
  agency_login: {{agency_login}}
  agency_pass: {{agency_pass}}
}

headers {
  apiKey: {{apiKey}}
  apiToken: {{apiToken}}
}
